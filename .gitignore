# 의존성
node_modules/

# 빌드 산출물
dist/
dev-dist/

# 디자인 생성 중간 산출물 (41MB) — .pen 원본만 커밋, 추출 이미지는 로컬 보관
/images/

# 환경변수 — 시크릿이 들어가므로 절대 커밋 금지 (.env.example만 커밋 허용)
.env
.env.*
!.env.example

# 로그
logs/
*.log

# 테스트 커버리지
coverage/

# 인증서·개인키 — 절대 커밋 금지
*.pem
*.key
*.p12
*.pfx

# TypeScript 증분 빌드 캐시
*.tsbuildinfo

# 에디터 / IDE
.vscode/
.idea/

# 린트 캐시
.eslintcache

# OS / OneDrive·Office 임시 파일
Thumbs.db
desktop.ini
.DS_Store
~$*
*.tmp

# 로컬 전용 설정
.claude/settings.local.json
